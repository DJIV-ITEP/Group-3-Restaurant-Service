apiVersion: v1
kind: Secret
metadata:
  name: postgres-customer-credentials
  namespace: itep-g3
stringData:
  postgres_user: "pscustomer"
  postgres_password: "ToorDerS"  # Replace with actual password (base64 encoded if needed)
type: kubernetes.io/opaque
